function main(){
    // Only change code belowe this line
    var lastName = "Tesla";
    var lastLetter = lastName[lastName.length - 1]; // lastLetter is "a"
    // Only change code above this line
    return lastLetterOfLastName;

}

console.log());
module.exports = main;